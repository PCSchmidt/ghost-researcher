# MEMORY_EPISODIC.md

Blueprint v11 | Session Log and Gate Outcomes

Appended by writethru-episodic hook on Stop events.
Updated at gate close with gate outcome rows.

## SESSION LOG

| Date | Project | Gate | Outcome | Tests | Notes |
| --- | --- | --- | --- | --- | --- |
| 2026-05-28 | GhostResearcher | v0.1.0 | IN PROGRESS | n/a | Reconstructed repo from prototype docs and created missing Gate 1 artifacts |
| 2026-05-29 | GhostResearcher | Gate 1 | CONFIRMED | 41 passing | Cost ceilings locked; AGENT_SPEC critical review completed; OpenRouter-first model routing accepted |
| 2026-05-30 | GhostResearcher | v0.8.0 | COMPLETE | 48 passing | Added deterministic web_search skeleton, queued source candidates, and search-first URL-free sequence |
| 2026-05-30 | GhostResearcher | v0.9.0 | COMPLETE | 54 passing | Added fake-tested OpenRouter planner adapter, prompt templates, validation, and model usage accounting |
| 2026-05-30 | GhostResearcher | v0.10.0 | COMPLETE | 63 passing | Added report schema, source-trace validation, synthesizer skeleton, and API synthesis serialization |
| 2026-06-01 | GhostResearcher | v0.11.0 | COMPLETE | 68 passing | Added research repository boundary, job IDs, JSON-file durability, and `GET /research/{job_id}` |
| 2026-06-01 | GhostResearcher | v0.12.0 | COMPLETE | 73 passing | Added persisted status events and `GET /research/{job_id}/events` SSE stream |
| 2026-06-01 | GhostResearcher | v0.13.0 | COMPLETE | 73 backend, 8 frontend | Added Next.js research workbench, EventSource status view, report/source cards, and CORS settings |
| 2026-06-02 | GhostResearcher | v0.14.0 | COMPLETE | 77 backend, 8 frontend | Added offline eval runner, benchmark scoring, source-trace checks, and persisted 3-prompt eval artifact |
| 2026-06-02 | GhostResearcher | v0.15.0 | COMPLETE | 80 backend, 8 frontend | Added multi-source deterministic planning, executable `finalize_report`, and 3-prompt eval artifact with source counts met |
| 2026-06-02 | GhostResearcher | v0.16.0 | COMPLETE | 85 backend, 8 frontend | Added search provider boundary, Brave adapter, eval `--mode offline|live`, and mode-labeled eval artifact |
| 2026-06-02 | GhostResearcher | v0.17.0 | COMPLETE | 90 backend OK, 8 frontend | Added skipped-by-default live smoke tests for Brave Search, OpenRouter planner/synthesizer, and CloakBrowser health/navigation |
| 2026-06-03 | GhostResearcher | v1.0.0 | COMPLETE | 89 backend, 8 frontend | Deployed backend to Railway (ghostresearcher-api + cloakserve) and frontend to Vercel; RAILWAY_REQUEST_TIMEOUT=300 set |
| 2026-06-04 | GhostResearcher | v1.0.1 | COMPLETE | 89 backend, 8 frontend | Fixed CDP Host header rewriting, DNS-rebinding bypass, HTTP readiness polling, wait_for guard, synthesis gate removal, evidence auto-creation |
| 2026-06-05 | GhostResearcher | v1.1.0 | COMPLETE | 89 backend, 8 frontend | Deep research operational: Brave Search, rich planner prompt, evidence from navigate results, LLM synthesis — full report rendered in frontend |
| 2026-06-08 | GhostResearcher | v1.2.0 | IN PROGRESS | 105 backend, 11 frontend | Reviewed 3 Codex branches (sound, on-plan); fixed compose/deploy Dockerfile refs; made offline eval discriminate via integrity/quality split (was tautological flat 1.0); surfaced bp_004/bp_007 as under-specified |
| 2026-06-09 | GhostResearcher | v1.2.0/v1.2.1 | IN PROGRESS | 110 backend, 11 frontend | Ran first live eval (avg quality 0.398); found deployed cloakserve was vanilla Chromium not CloakBrowser → planned v1.2.1; built Phase 1 (cloakserve launches CloakBrowser stealth binary, blocked_rate harness); Linux container de-risk passed; merged (PR #7) + deployed to Railway; datacenter-IP block rate ~100% → 0.375 (5/8 usable); production research run now returns 0.68-confidence report (5 findings, 7 sources, was empty). Phase 1 DONE; DEC-010 resolved (option 1 no proxy; option 3 proxy-on-retry optional Phase 2) |
| 2026-06-10 | GhostResearcher | v1.2.2 patch | COMPLETE | 114 backend, 12 frontend | Diagnosed an intermittent production `POST /research` 500 (re-run raised mid-loop after ~140s; runs were an hour apart so not concurrency, 140s<300s so not an edge timeout). The whole job runs synchronously and the planner loop was unwrapped, so any flaky call (suspected stale shared-browser CDP) 500'd and discarded all gathered evidence — with no traceback logged and the frontend showing only "status 500". Fixed: (1) wrapped the planner loop + synthesis so a mid-run failure finalizes ("error") and synthesizes a partial report instead of 500ing; (2) added a stderr log handler on the `ghostresearcher` namespace so future failures leave a traceback in Railway logs; (3) frontend now surfaces the server `detail`. Also fixed a latent bug: the max_steps-exhausted branch guarded on `if not termination_state` but the default is the truthy "active", so finished runs were never finalized (sidebar showed STATE "active"); now guards on `== "active"` → finalizes "max_steps". Merged to main (38c4f8e), deployed + re-verified in production (two clean 200s, 0.75-confidence 10-finding report) |
| 2026-06-10 | GhostResearcher | v1.2.3 | COMPLETE | 121 backend, 13 frontend | Mobile returned "Failed to fetch": the whole job ran synchronously inside POST /research (~100-200s) and mobile browsers drop long-pending fetches. Made it async — POST persists a "running" job and returns immediately (verified prod: 0.11s), work runs detached via asyncio.create_task, client polls GET /research/{job_id} every 2.5s until terminal; repository gained update(job_id,...). Added live progress (progress_callback persists an in-progress snapshot per step via build_status_events(final=False)) so Steps/Sources climb and agent steps stream live; ReportViewer shows "Researching…" until coverage. asyncio.Lock serializes the shared CloakBrowser (concurrent submits queue). Hard per-job timeout JOB_HARD_TIMEOUT_SECONDS (default 330) via asyncio.wait_for cancels a hung job to terminal error (the in-loop budget only checks between steps). Single uvicorn worker keeps the in-memory repo coherent for polling; Redis queue is the documented multi-worker upgrade. Merged to main (74dd93c, async core) + follow-up commit (progress/timeout/docs); deployed Railway+Vercel |
| 2026-06-10 | GhostResearcher | v1.5.0 core | SHIPPED (LIVE) | 128 backend, 14 frontend | Long-Form Research Report core, built in increments behind LONGFORM_ENABLED: (1) ResearchReport v2 schema — additive abstract/sections(paragraphs+in-text citations)/conclusion/references; source-trace validation extended to section citations + references; (2) multi-pass synthesizer — outline (premium=fallback model) → per-section drafting (cheap=default model, drafted CONCURRENTLY via asyncio.gather) → framing (premium); any pass failure degrades to a deterministic long-form build; (3) frontend ReportViewer renders the paper (abstract, numbered sections with [n] citation markers, references), falls back to flat when no sections. Enabled in prod (LONGFORM_ENABLED=true, MAX_MODEL_COST_PER_JOB_USD=0.75, JOB_TIME_BUDGET_SECONDS=150, JOB_HARD_TIMEOUT_SECONDS=420). DIAGNOSIS: first live runs hit the hard timeout — the cloakserve browser was the SAME instance alive for days; research hung on the stale shared browser (in-loop budget can't preempt a hung await). Restarted CloakBrowser (fresh guid) → research climbed steps 0→11, multi-pass synthesis produced a 4-section paper (real abstract, in-text citations, 5 refs, 6 findings, conf 0.54) in ~315s. Merged 7b8d3b4 + perf commit 2f6b851 (concurrent sections). REMAINING: evidence-coverage eval metric; recurring operational fix — periodically recycle the long-lived cloakserve browser; record synthesis cost (running_cost stayed 0 — OpenRouter usage.cost not populated for these models) |
| 2026-06-10 | GhostResearcher | browser self-healing | DONE (LIVE) | 129 backend, 14 frontend | Made the stale-shared-browser hang self-healing (the chronic root cause behind the v1.5.0 timeouts; the in-loop budget can't preempt a hung await). Job-level (executor/navigate.py): bound connect_over_cdp (20s), bound browser.close()/playwright.stop() cleanup (5s), and wrap the whole navigation in an overall timeout (3x page timeout) — a degraded browser now makes a nav FAIL FAST (blocked, skippable) so the planner moves on and the job completes, instead of hanging to the hard cap and getting wiped. Browser-level (scripts/start_cloakserve.py): opt-in periodic recycle — after CLOAKSERVE_MAX_AGE_SECONDS, terminate + relaunch Chromium on the same port (one browser at a time, retries, never browserless); default 0 (off) so deploy is behavior-neutral. VERIFIED live: set =120 temporarily, browser guid rotated every ~110-120s while cloak stayed ok (no downtime), then set prod =21600 (6h); a full long-form run completed cleanly (3-section paper, steps 0→13, 370s). Merged 3462fc6 |
| 2026-06-11 | GhostResearcher | v1.5.0 evals + v1.3.0 | SHIPPED | 135 backend, 16 frontend | Closed v1.5.0 with the evidence-coverage + supported-claims eval metrics (DEC-011: score by evidence not pages): evidence_coverage_score = cited∩gathered / gathered; supported_claims_score = claims/paragraphs with ≥1 citation / total; folded into quality_score (7 components); `--longform` offline flag exercises the deterministic long-form build; artifact eval_results_20260611T172841Z.json (avg 0.905). Then built v1.3.0 Shareable Report (client print-to-PDF, DEC-008): repository.list() + GET /reports summaries; frontend /reports/[id] permalink renders a ReportDocument academic paper layout (serif, numbered sections, in-text [n] citations, references) with a Download/Print PDF button (window.print()); /reports list; print CSS (@media print) strips chrome → that IS the PDF layout; workspace gains Open report / All reports links. Merged a6966ea (evals) + 5181511 (v1.3.0). CAVEAT to address: default repo is IN-MEMORY → permalinks/reports do NOT survive an API redeploy; switch prod to JsonFile/Postgres for durable shareable links. Deferred: social OG/Twitter preview cards; optional server-rendered PDF (cloakserve page.pdf()) |
| 2026-06-11 | GhostResearcher | durable reports + fixes | DONE (LIVE) | 137 backend, 16 frontend | Investigated a prod run that "didn't complete": it gathered 6 sources but returned an empty report — research+long-form synthesis share one MAX_TOKENS_PER_JOB (125000); the research loop spent it, synthesis hit the cost guard, raised cost_limit, and _synthesize_if_ready swallowed it to None. Fixed: synthesize() degrades to a token-free deterministic build when over budget or a model pass fails (a run with evidence never returns empty); raised prod MAX_TOKENS_PER_JOB=500000 so the model paper normally succeeds; added ghostresearcher.synthesis logging so the next model-synthesis fallback is diagnosable (was silent). UX: research goal now persisted to localStorage (survives navigating to a report and back). Then built DURABLE PERSISTENCE: REPORTS_DB_PATH → JsonFileResearchRepository in create_app; attached a Railway volume at /data on ghost-researcher, REPORTS_DB_PATH=/data/reports.json. VERIFIED in prod: ran a job (model paper "Retrieval-Augmented Generation with Vector Databases…", 4 sections), redeployed the API, report + permalink + /reports SURVIVED the restart. Commits c9e9e59 (logging+prompt), 0cd3882 (empty-report fix), f358a31 (durable repo). CLI note: `railway volume add -m /data` mangles the path under Git Bash (use MSYS_NO_PATHCONV=1) and panics on its prompt unless given a service ID + stdin |

| 2026-06-12 | GhostResearcher | social previews + fallback log | DONE | 137 backend, 21 frontend | Shipped social-preview cards for `/reports/[id]` (the deferred v1.3.0 follow-up). Crawlers don't run JS, so the permalink route was converted from a client component to a SERVER component: `generateMetadata` fetches the report server-side and emits real Open Graph + Twitter (`summary_large_image`) tags, and a colocated `opengraph-image.tsx` renders a 1200×630 branded PNG card (title + confidence/source/"Research paper" stats) via `next/og`/Satori; interactive bits (fetch, print-to-PDF) moved to a new `ReportPermalinkView` client child. `metadataBase` set in root layout from `NEXT_PUBLIC_SITE_URL` (defaults to the prod Vercel URL) so image URLs resolve absolute. Both paths resilient: API-down / missing report → generic branded card + metadata (verified locally — `og:image` absolute, 53KB PNG, fallback card renders). Also closed the passive synthesis-fallback gap: the over-budget skip (synthesis skipped because the research loop already spent the token budget — the exact "came back deterministic" case) was SILENT; now logs a `ghostresearcher.synthesis` WARNING with reason + token/cost figures (the mid-attempt SynthesisError path already logged). New: `frontend/src/lib/reportSummary.ts` (+test), `ReportPermalinkView.tsx`, `opengraph-image.tsx`. NOT yet committed/deployed — awaiting deploy go-ahead. |

| 2026-06-12 | GhostResearcher | planner-call timeout (ERR-010) | DONE | 138 backend, 21 frontend | Diagnosed a prod `job_timeout` on a large goal (job a90b4ac0, STEPS/SOURCES 0): the job acquired the browser lock then hung 420s with `planner_turns=0, tokens=0, search_queries=[]`, only `job_started`→`job_failed`, and ZERO `ghostresearcher.navigate`/`synthesis` logs. `run_sequence` step 0 is `_plan_next` (the OpenRouter call) BEFORE any browser op, so this was NOT the stale-browser issue — confirmed by health OK (Chrome/146) and a fresh test job running clean to a 0.55-confidence long-form paper (30 turns, 9 sources). ROOT CAUSE: a single model call had no overall wall-clock bound — the blocking `urlopen(timeout=30)` runs in a worker thread, but OpenRouter's keep-alive `: OPENROUTER PROCESSING` bytes defeat the per-socket timeout, so a slow/stuck first generation (more likely on a huge/broad goal) ran to the 420s hard cap with zero progress. FIX: `OpenRouterChatClient.complete` now wraps the `to_thread` await in `asyncio.wait_for` bounded by `MODEL_CALL_TIMEOUT_SECONDS` (default 90s); on timeout raises `PlannerAdapterError("openrouter_timeout")` → planner loop finalizes on gathered evidence, synthesizer degrades to deterministic (broadened its flat-path except to catch it too). One stuck generation now fails fast (~90s) instead of hanging 420s. Logged ERR-010, `.env.example` MODEL_CALL_TIMEOUT_SECONDS, new bound test. NOT yet committed — awaiting deploy go-ahead. |

## STOP EVENTS

[Appended automatically by writethru-episodic hook]
